export {
  applyTheme,
  readThemePreference,
  resolveTheme,
  setThemePreference,
  startThemeSync,
  THEME_COLORS,
  THEME_STORAGE_KEY,
  themePreferences,
  type Theme,
  type ThemePreference,
} from './theme';
export { useResolvedTheme, useThemePreference } from './use-theme';
