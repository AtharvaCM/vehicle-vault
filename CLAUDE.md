# vehicle-vault

Monorepo: NestJS API (`apps/api`) + web frontend (`apps/web`).

## Agent skills

### Issue tracker

Issues live as GitHub issues in `AtharvaCM/vehicle-vault`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Product context

Product/domain briefing (what the product is, personas, shipped timeline, which docs to trust): `docs/product-context.md`. Read before roadmap/scoping work.

### Domain docs

Multi-context monorepo — `CONTEXT-MAP.md` at root, per-app `CONTEXT.md` under `apps/api/` and `apps/web/`. See `docs/agents/domain.md`.
