node_modules
dist
dist-ssr
coverage
pnpm-lock.yaml

# Generated by semantic-release on every release commit, in its own markdown
# style (`*` bullets). Formatting it here would re-drift on the next release
# and break the CI format check.
CHANGELOG.md
